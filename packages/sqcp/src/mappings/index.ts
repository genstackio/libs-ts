import standard from './standard';
import gotombola from './gotombola';
import identity from './identity';
import utm from './utm';
import { sqcp_mappings } from '../types';

const mappings: sqcp_mappings = {
    standard,
    gotombola,
    identity,
    utm,
};

export default mappings;
